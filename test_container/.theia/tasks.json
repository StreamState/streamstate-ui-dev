{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run spark streaming job",
            "type": "shell",
            "command": "./scripts/run.sh",
            "args": [],
            "options": {
                "cwd": "${workspaceFolder}"
            }
        },
        {
            "label": "Save inputs and outputs",
            "type": "shell",
            "command": "./scripts/generate_test_topics.sh",
            "args": [],
            "options": {
                "cwd": "${workspaceFolder}"
            }
        }
    ]
}